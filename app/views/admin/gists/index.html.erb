<div class="container p-3">
  <div class="row">
    <div class="col justify-content-center">
      <h1 class="text-center mt-3 mb-4"><%= t('.gist_list') %></h1>
        <table class="table">
          <thead class="table-dark">
            <tr>
              <th scope="col" class="text-center">#</th>
              <th scope="col" class="text-center"><%= t('.gist_user') %></th>
              <th scope="col" class="ml-3"><%= t('.gist_question') %></th>
              <th scope="col" class="text-center"><%= t('.gist_link') %></th>
              <th scope="col"><%= t('.gist_action') %></th>
            </tr>
          </thead>

            <%= render @gists %>

        </table>
    </div>
  </div>
</div>
