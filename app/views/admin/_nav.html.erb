 <header>
   <div class="container-fluid">
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <span class="navbar-brand mr-auto ml-5">
        <% if user_signed_in? %>
          <% if current_user.admin? %>
            <%= link_to root_path do %>
              <i class="fa fa-graduation-cap" aria-hidden="true"><%= t('.apanel')%></i>
            <% end %>
          <% else %>
            <%= link_to root_path do %>
              <i class="fa fa-graduation-cap" aria-hidden="true">TestGuru</i>
            <% end %>
          <% end %>
        <% else %>
            <%= link_to root_path do %>
              <i class="fa fa-graduation-cap" aria-hidden="true">TestGuru</i>
            <% end %>
        <% end %>
      </span>

      <span class="navbar-nav ml-auto mr-5">
          <%= link_to 'Eng', { lang: :en }, class: "mr-1 ml-1 font-weight-light btn btn-outline-secondary#{I18n.locale == :en ? ' disabled' : nil }"%>
          <%= link_to 'Рус', { lang: :ru }, class: "mr-1 ml-1 font-weight-light btn btn-outline-secondary#{I18n.locale == :ru ? ' disabled' : nil }"%>
          
        <% if user_signed_in? %>
          <span class="ml-3 mr-2 mt-1 font-weight-light"><%= t('.welcome') %><b class="ml-2"><%= current_user.email %></b> Guru</span>
          <%= link_to destroy_user_session_path, method: :delete, class: "ml-2 btn btn-outline-secondary" do %>
            <i class="fa fa-sign-out" aria-hidden="true"><%= t('.logout') %></i>
          <% end %>
        <% else %>
          <%= link_to "Log in", new_session_path(resource_name), class: "ml-5 mr-1 btn btn-outline-info" %>
          <%= link_to "Sign up", new_registration_path(resource_name), class: "ml-1 mr-1 btn btn-outline-info" %>
        <% end %>
      </span>
    </nav>
  </div>
</header>
