<% if user_signed_in? %>
  <div class="nav nav-admin">
    <% if current_page?(root_path) %>
      <% if admin? %>
        <span class="nav-admin title">TestGuru Admin Panel</span>
      <% else %>
        <span class="nav-admin title">TestGuru</span>
      <% end %>
    <% else %>
      <% if admin? %>
        <span class="nav-admin title">
          <%= link_to 'TestGuru Admin panel', root_path%>
        </span>
      <% else %>
        <span class="nav-admin title">
          <%= link_to 'TestGuru', root_path%>
        </span>
      <% end %>
    <% end %>
    <div class="nav-admin user">
      Welcome, <b class="nav-admin user-email"><%= current_user.email %></b> Guru |
      <%= link_to 'Logout', destroy_user_session_path, method: :delete %>
    </div>
  </div>
<% end %>
